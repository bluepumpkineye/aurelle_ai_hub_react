# Aurelle engine + API — pinned to the versions the app is verified against.
# CPU-only PyTorch (no CUDA) keeps the container small enough for free hosts.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0

# Engine
streamlit==1.55.0
openai==1.68.2
langchain==0.3.25
langchain-openai==0.3.18
langchain-community==0.3.24
faiss-cpu==1.13.2
pandas==2.3.3
matplotlib==3.10.8
numpy==2.4.2
plotly==5.22.0
scikit-learn==1.8.0
python-dotenv==1.0.1
tiktoken==0.12.0
pydantic==2.12.5
httpx==0.28.1
truststore==0.10.4
sentence-transformers==5.3.0
lightrag-hku==1.4.16

# API layer (FastAPI + ASGI server)
fastapi==0.135.3
uvicorn[standard]==0.44.0

# NOTE: the optional Atelier governance core (atelier-mcp) is intentionally not
# listed — it lives in a private repo and the app falls back to a local
# governance envelope when it is absent (see governance_client.py).

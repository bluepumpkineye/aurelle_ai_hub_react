# DEVIATIONS.md — Aurelle Boutique Planner

Per PRD operating instructions: infeasible or deferred items land here with the
nearest feasible alternative that shipped, and the upgrade path.

| # | PRD item | What shipped instead | Upgrade path |
|---|----------|----------------------|--------------|
| D-1 | Reference images in `reference/` (5 shots) drive the reference-delta loop | The folder was never provided with the repo. The delta loop was run against the **textual art targets** recorded in STATUS.md ("Reference image analysis") using live screenshots at the authored viewpoints. | Drop the five images into `reference/`; wire `tools/compare.ts` side-by-sides. |
| D-2 | Playwright shot/compare harness (`tools/shoot.ts`, `tools/compare.ts`, `battery.ts`) | The verification battery ran as **scripted console evals** against the `window.__aurelleVM` hook (exposed by `web/src/vm/VMController.ts`): planogram round-trip ✓ exact, full-floor bulk update 24.7 ms ✓, signal propagation 17.1 ms ✓, undo/redo ✓, aisle-guard rejection ✓, MeshBasicMaterial audit ✓ (`scene.auditMaterials()`), all-overlays co-visibility ✓ (visual). | Port the same evals into a Playwright script (`chromium.launch({channel:'chromium'})`, probe on `http://localhost:5173`, never `about:blank`). |
| D-3 | TAA, GTAO, GI probe grid (8×8×4/zone), screen-space bounce, caustic approximation, CSM×3 + PCSS | Post stack ships **ACES tone mapping + HDR bloom + Aurelle warm/cool split-toning + vignette** (`core/Engine.ts`). Shadows are PCF-soft spot/directional maps (2–6 casters by quality preset) + per-fixture radial **contact-shadow decals**. Case interiors are filled by emissive LED baffle geometry + per-case warm point lights (high preset). IBL comes from a procedural equirect interior environment. | TRAANode/GTAONode exist in the pinned three (verified in THREE-NOTES). Add behind the `high` quality preset; GI probes via `Storage3DTexture`; caustics as screen-space disc projection per the STATUS note. |
| D-4 | Physical glass with transmission everywhere | Transmission triggers a **full extra scene pass per frame**; it is enabled on the `high` preset only. `balanced`/`igpu` presets use fresnel-transparent glass (IOR kept at 1.52 for specular response). See `render/Materials.ts`, `core/QualityPresets.ts`. | None needed — preset-gated by design. |
| D-5 | Triangle floors: ≥500k showcase close-up / ≥200k full-floor | Current scene renders ~35k–120k triangles in view (HUD-verified). Geometry is parametric, so density is a knob — but raising it before the perf budget exists on iGPU would violate the 30 fps field target. | Raise segment counts on marble floor, products and fixture fillets under the `high` preset once TAA lands. |
| D-6 | ≥64 active spots per zone | Forward lighting cost is per-pixel × per-light: spot budget is **8 (igpu) / 18 (balanced) / 32 (high)**, priority-sorted from fixture lighting attachments; remaining fixtures still read lit via emissive lenses/baffles. Measured on Intel Gen12: 29 fps @ ~300 draws with 8 spots + 2 shadow casters. | Clustered/tiled lighting via TSL compute, or `light-track` consolidation. |
| D-7 | Product mesh LOD (full detail <1 m, silhouette >2 m, billboard in overview) | Single merged low-poly mesh per product (2–4 draw calls each), no LOD pop by construction. | Add distance-swapped geometry with dithered transitions if product counts grow. |
| D-8 | Specimen gallery scene (`?scene=gallery`) | Not built. The fixture library panel + two live boutiques serve as the review surface. | Add a `?scene=gallery` route rendering all 20 templates × 2 configs + 9 product categories × 3 SKUs on labeled pedestals. |
| D-9 | Photo-mode DoF (aperture + focus distance) | Not shipped; DepthOfFieldNode is available in the pinned three but costs another pass on iGPU. Six authored bookmarks + walk mode + smooth 0.8 s transitions shipped. | Preset-gate a DoF pass behind `high`. |
| D-10 | Command palette planogram action | Palette finds planograms but points the user to the fixture panel / Bulk Update modal rather than opening a pre-loaded modal. | Thread a `openBulkModal(planogramId)` callback through VMApp. |

Session-persistence contract honored as specified: all state is in-memory,
survives in-session navigation, resets on reload. No localStorage was added.
